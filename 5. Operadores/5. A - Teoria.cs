using System;

namespace Aritmeticos

{

	public class Teoria
	{
		public Teoria()
		{
            // soma : +
            // subtração : -
            // multiplicação: *
            // divisão: /
            // módulo : %

            // Biblioteca Math
            // Math.PI = representa o número pi
            // Math.E = representa a base e do logaritmo natural
            // Math.Cos(x) = obtém o cosseno de x
            // Math.Sin(x) = obtém o seno de x
            // Math.Tan(x) = obtém a tangente de x
            // Math.Sqrt(x) = obtém a raiz quadrada de x
            // Math.Pow(x,y) = obtém a potência de x elevado a y
            // Math.Abs(x) = obtém o valor absoluto de x
            // Math.Max(x,y) = obtém o maior valor entre dois números
            // Math.Min(x,y) = obtém o menor valor entre dois números
            // Math.Log10(x) = calcula o logaritmo de x na base 10
            // Math.Log(x) = calcula o logaritmo de x na base e
            // Math.Exp(x) = retorna o exponencial (e elevado a x)
        }
    }


}